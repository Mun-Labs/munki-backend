async fn calcualte_value(btc_fear: i32, sol_price_change: i32, blochain_volume: u64) -> i32 {
    0
}

#[cfg(test)]
mod test {
    #[test] 
    fn test_calcualte_value() {
        println!("hello {}", 3/2);
    }
}
